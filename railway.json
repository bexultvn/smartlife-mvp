{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build:css && npm run build",
    "outputDirectory": "dist"
  },
  "deploy": {
    "startCommand": "echo 'Static site - served by Railway nginx'"
  }
}
